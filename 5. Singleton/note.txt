When discussing which patterns to drop, we found that we still love them all. (Not really— I'm in favor of dropping Singleton. Its use is
almost always a design smell.)

For some components it only makes sense to have one in the system
• Database repository
• Object factory
E.g., the construction call is expensive
• We only do it once
• We give everyone the same instance
Want to prevent anyone creating additional copies
Need to take care of lazy instantiation

Singleton
a components which is instantiated only once 

Lazy one-time initialization using sync. Once
Adhere to DIP: depend on interfaces, not concrete types
Singleton is not scary 